 <view class="nm">物资信息登记系统</view>

<view class="ipt">
  <input bindinput="changephone" placeholder="公司名称" value="{{phone}}" />
</view>
<view class="ipt">
  <input bindinput="changepwd" placeholder="密码" value="{{pwd}}" />
</view>
<view class="btn" bindtap="submit">
  登 录
</view>